# 💧 喝了么 — 喝水追踪器

一个轻量、美观的每日饮水追踪工具。支持两种形态：

- **☁️ 云端共享版（推荐）**：`index.html` 部署到 **Vercel**，数据存 **Supabase** 云数据库，任何人通过链接即可注册使用，数据全局共享
- **💻 本地单机版**：`water-tracker.html` 双击即用，数据存浏览器 localStorage

> 📦 部署云端版请看 **[DEPLOY.md](DEPLOY.md)**（约 10 分钟完成）。

---

## ✨ 功能一览

### 🔐 用户系统（云端版）

- **注册/登录**：输入用户名 + 密码，首次输入自动创建账号
- **密码安全**：PBKDF2（10 万次迭代 + 随机盐）哈希存储，不存明文
- **会话缓存**：同设备下次打开自动登录
- **数据隔离**：每位用户只能看到自己的喝水数据
- **后台管理**：登录页底部「🛠 进入后台管理」，可查看所有用户统计、重置密码、删除用户、全量备份/恢复

### 快速记录

- 四种预设容量一键记录：小杯 150ml / 标准杯 250ml / 大杯 400ml / 水瓶 500ml
- 自定义毫升数输入，回车即可添加
- 每条记录自动标注当前时间
- 卡片右上角「📅 补录历史」入口

### 今日进度

- 环形进度图实时展示今日饮水量 / 目标 / 完成百分比
- 达成目标自动弹出庆祝提示 🎉
- 显示完成度、剩余量、记录次数

### 每日目标

- 默认 2000ml，支持 +/− 每次调整 100ml
- 范围限制 500ml ~ 10000ml

### 今日记录管理

- 按时间倒序排列，显示杯子图标 + 时间 + 毫升数
- 内联编辑：点击 ✎ 直接修改毫升数，Enter 保存 / Esc 取消
- 单条删除、一键清空今日（带确认弹窗）

### 近 7 天趋势

- 柱状图展示最近一周每日饮水量
- 今日柱条高亮，达标日显示绿色

### 📅 当月日历视图

- 颜色分级：灰色（无记录）/ 蓝色（有记录）/ 浅绿（过半）/ 深绿（达标）
- 月份切换、月度统计（合计/天数/日均/达标）
- 点击任意历史日期直接补录，今日高亮，未来日期不可点击

### 历史记录查看

- 按天折叠显示、分页浏览（10/20/50 条每页）
- 时间范围筛选：最近一周 / 一月 / 三月 / 全部 / 自定义

### 历史日期补录

- 弹窗选择任意历史日期和时间补录
- 实时预览所选日期已有记录和合计
- 历史记录、日历中均可直接补录

### 数据管理

- **云端同步**：所有操作实时写入 Supabase，任何设备登录同一账号数据一致
- **📤 导出（用户级）**：下载当前用户数据为备份文件
- **📥 导入（用户级）**：从备份文件恢复，可用于旧版数据迁移
- **📤📥 全量导出/导入（管理员）**：备份/恢复所有用户数据，导入带预览确认

---

## 🖥️ 页面布局

双列响应式布局：

```
┌─────────────── 标题栏（用户徽章/导出/导入/登出）──────────────┐
├──────────────────┬───────────────────────────────────┤
│     左 列        │             右 列                  │
│  💧 今日进度环    │  🎯 每日目标                       │
│  🚰 快速记录      │  📊 近 7 天趋势                   │
│  📝 今日记录      │  📅 当月日历                       │
├──────────────────┴───────────────────────────────────┤
│                     页脚                              │
└──────────────────────────────────────────────────────┘
```

- 宽度 > 860px：双列并排；≤ 860px：单列上下排列；≤ 480px：按钮两列

---

## 📁 文件说明

| 文件 | 说明 | 是否必须 |
|------|------|----------|
| `index.html` | **云端版应用**（Vercel 部署入口，部署前填 Supabase 配置） | ✅ 必须（云端版） |
| `supabase/schema.sql` | 数据库建表脚本（Supabase SQL Editor 执行） | ✅ 必须（云端版） |
| `DEPLOY.md` | 云端版部署指南 | 推荐阅读 |
| `water-tracker.html` | 旧版本地单文件应用（localStorage 版） | 可选（离线备用） |
| `water-data.js` | 旧版初始数据文件 | 可选 |
| `README.md` | 项目说明文档 | 可选 |

---

## 🛠️ 技术实现（云端版）

### 数据模型（Supabase / PostgreSQL）

**profiles 表**（用户）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `username` | text (PK) | 用户名，登录标识 |
| `password_hash` | text | PBKDF2 密码哈希 |
| `goal` | integer | 每日饮水目标 |
| `first_login` / `last_active` | timestamptz | 登录时间追踪 |

**water_records 表**（喝水记录）：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | bigint (PK) | 自增主键 |
| `username` | text (FK) | 所属用户，级联删除 |
| `date` | date | 日期 YYYY-MM-DD |
| `time` | text | 时刻 HH:MM |
| `amount` | integer | 毫升数 |

### 架构要点

```
浏览器（index.html）
  ├─ 用户名+密码登录 → PBKDF2 校验 → 会话缓存 localStorage
  ├─ 内存数据缓存（同步渲染 UI）
  └─ 每次操作异步写 Supabase（增/删/改/查）
```

- **零后端**：Vercel 只托管静态文件，数据读写直接走 Supabase REST API（HTTPS）
- **内存缓存 + 异步同步**：UI 渲染沿用同步接口，数据落库为精确的增删改查
- **RLS 行级安全**：数据库开放读写，安全由应用层「用户名 + 密码」校验保证
  （适合亲友/团队内部使用；如需对公开放请改用 Supabase Auth）

### 技术栈

- 纯原生 HTML/CSS/JS，零框架
- Supabase JS SDK（CDN 引入，UMD 版）
- Web Crypto API（PBKDF2 密码哈希）
- 前端静态部署：Vercel

---

## 📋 使用流程（云端版）

1. 管理员按 **DEPLOY.md** 完成部署，把链接分享给大家
2. 用户打开链接 → 输入用户名 + 密码 → 自动注册/登录
3. 喝水后点击对应容量按钮记录，数据实时同步云端
4. 换设备登录同一账号，数据自动一致
5. 忘记密码 → 联系管理员在后台重置

---

## 📌 版本历史

| 版本 | 内容 |
|------|------|
| v1.0 ~ v10.2 | 本地单机版迭代：基础记录、进度环、日历、多用户、后台管理（localStorage） |
| **v11.0** | **云端共享版**：Vercel 静态托管 + Supabase 数据库，用户名+密码登录（PBKDF2），后台支持重置密码，全量数据云端备份/恢复 |
