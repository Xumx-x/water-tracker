# 2026-08-20 工作日志

## 喝水追踪器改造：本地版 → Vercel + Supabase 云端共享版

**用户决策**：用户名+密码登录（PBKDF2）、尚无 Supabase 项目（需完整指导）、旧数据不迁移。

**完成内容**：
- 新建 `index.html`：云端版应用（Vercel 部署入口）。核心改造：
  - 登录/注册改为用户名+密码，PBKDF2(100k 迭代+随机盐) 哈希，Web Crypto 原生实现
  - 数据层从 localStorage 改为 Supabase REST：内存缓存同步渲染 + 每次操作异步精确写库（增/删/改/查）
  - 记录主键改用数据库自增 id，操作全部映射 water_records/profiles 表
  - 后台管理改为数据库聚合（用户列表/汇总/7天日活），新增管理员重置密码功能
  - 未配置 Supabase 时登录页显示引导横幅
  - supabase-js 用 CDN UMD 引入，保持零构建、双击可用的单文件形态
- 新建 `supabase/schema.sql`：profiles + water_records 表、索引、RLS 策略（using(true)，应用层鉴权）
- 新建 `DEPLOY.md`：Supabase 建项目→执行 SQL→填配置→Vercel 部署全流程指南
- 更新 `README.md`：双版本架构说明（云端版推荐 / 本地版保留）
- 旧文件 `water-tracker.html`、`water-data.js` 保留不动

**验证**：内嵌 JS 语法检查通过；jsdom 冒烟测试通过（未配置/已配置两种场景均无报错）；HTML 标签配对完整。

**关键技术点**：
- PBKDF2 哈希格式 `pbkdf2$100000$salt$hex`
- PostgREST 删除全部记录用 `delete().neq('id', 0)` 技巧
- 全量备份格式向后兼容旧版（含 `_passwordHash` 字段保留密码）
- 旧格式备份导入时无密码字段 → 默认密码 water123（预览弹窗提示）

## 新增：🏆 喝水量排行榜（v11.1）

**需求**：右侧栏展示每日/每周/每月 Top 10 用户喝水量排名。

**实现**：
- `index.html` 右侧栏顶部新增 rank-card：每日/每周/每月 三个 tab（自然周=周一至今、自然月=1号至今，卡片显示统计区间）
- 前端拉取时段内 `username,date,amount` 明细 → JS 分组求和（**关键**：Supabase anon 默认禁用数据库聚合函数，PGRST123「Use of aggregate functions is not allowed」，不能直接 `select('username,total:amount.sum()')`，必须前端聚合或建 RPC）
- 排序取 Top 10，前三名 🥇🥈🥉，自己高亮 + 「我」标签，未进前十时底部显示自己的名次
- 刷新时机：登录/自动登录后 `loadRanking()`；addWater/deleteRecord/saveEdit/clearToday/backfillAdd 成功后 `refreshRanking()`（400ms 防抖）
- 并发防护：`rankState.reqId` 自增，旧请求返回后丢弃，避免 tab 快速切换串数据

**验证**：语法检查通过；jsdom 冒烟测试覆盖「自己第11名/自己进前十/tab切换/空数据/日期过滤」5 个场景；真实 Supabase 只读探测确认聚合被禁 + 表已存在有数据。

**注意**：真实库已有一条测试记录（id=1），用户已建表；排行榜上线后即可看到真实数据。

## 布局重构：Gitee 风格导航栏 + 三页面路由（v11.2）

**需求**：全局固定 48px 深色导航栏（logo/首页+历史记录菜单/用户昵称+下拉设置+退出）；主体居中容器 max-width；当前页菜单高亮、点击路由跳转；趋势图迁到历史页改为近 30 天。

**实现**：
- 导航栏 `.topbar`：fixed 顶部、深色 #1f2733、z-index 900（低于弹窗 2000+/登录 5000）；inner 同 1200px 容器对齐
- hash 路由 SPA：`#/home` `#/history` `#/settings`；`goPage()/renderRoute()/hashchange`；`.page.active` 切换 + `.nav-link.active` 高亮（蓝色下划线）
- 首页 = 进度环/快速记录/今日记录 + 排行榜/日历；历史页 = 近 30 天趋势图（#trendChart，数值标签仅今天+每5天，hover title 显示日期）+ 原历史弹窗内容内联化（hm-* 样式复用 + history-card 容器）；设置页 = 每日目标 goal-card + 数据导出/导入按钮
- 用户区：`navUser`（点击下拉，document click 关闭）+ 独立 `navLogout` 按钮；renderUserBadge/doLogout 改为操作导航栏元素
- 删除：旧 .header 标题块、goal-card/chart-card（右栏）、历史弹窗 HTML 与相关 CSS、showHistory/closeHistory
- 细节：登录成功 goPage('home')；首次点自定义筛选预填最近 30 天；backfillAdd 后用 `currentPage()==='history'` 判断刷新；body padding-top 48px

**验证**：语法检查 + 标签配对（grep 统计，bash 里 `\\s` 会被吃掉导致正则误报，要用 grep -o 计数）；jsdom 测试覆盖导航栏渲染/下拉开关/三页切换/30根柱/历史行渲染/设置页，全部通过。

**坑**：jsdom 的 window.scrollTo 未实现会打印 Not implemented 警告，无影响。

## 功能增强四连（v11.3，不更新交付文档）

**需求**：①每日目标支持手动输入 ②历史记录支持删除单条 ③设置页新增密码修改 ④部署平台文案 Vercel→Netlify（用户实际用 Netlify）。

**实现**：
- 目标输入：goal-card 的 `<span id="goalValue">` 改为 `<input id="goalInput">` + ml 单位；`changeGoal` 重构为 `saveGoal(goal)`（范围钳制 500-10000 + 失败回滚）+ `applyGoalInput()`（Enter/onchange 触发，非法值回滚提示）；`syncGoalInput()` 同步输入框；renderProgress/renderRoute 移除旧 goalValue 引用（**必须同步删，否则 getElementById 返回 null 抛 TypeError**）
- 历史删除：renderHistory 每条 day-record 加 `.dr-rec-del` 删除按钮（hover 变红）；`deleteRecord` 改为遍历缓存**所有日期**过滤该 id（原实现只删今天 cache[today]），删空某天则移除日期键，删除后 `currentPage()==='history'` 时 renderHistory() 刷新
- 密码修改：设置页新增 settings-card（pwOld/pwNew/pwNew2 + 确认按钮，Enter 提交）；`changePassword()`：校验旧密码（verifyPassword 查库）→ 校验新密码≥6位与两次一致 → hashPassword 新哈希 → update profiles.password_hash；成功清空表单
- Netlify：文件头注释 + footer 两处 Vercel 改 Netlify（README/DEPLOY 按用户要求不动）

**验证**：语法检查通过；jsdom 冒烟测试全绿（Netlify 文案/手动输入与非法回滚/密码三场景含新旧密码校验闭环/跨日期删除与删空键移除）。
**坑**：jsdom 的 `window.crypto` 只读且无 subtle，直接赋值会被静默忽略导致 crypto.subtle undefined → 测试注入须用 `Object.defineProperty(window,'crypto',{value: crypto.webcrypto, configurable:true, writable:true})`；真实浏览器 HTTPS 下 crypto.subtle 始终可用，不影响生产。

## 快速记录自定义（v11.3）+ 喝友论坛（v11.4）

**需求（自定义）**：设置页自定义首页快速记录按钮，最多 8 个；每人默认仍是 4 个（150/250/400/500ml）；首页每行 4 个、超出换行。
**实现（自定义）**：
- `DEFAULT_CUPS` 常量 + `getCups()`（custom_cups 优先）+ `getCupIcon(amount)`（历史列表图标跟随自定义）
- profiles 表新增 `custom_cups`（JSON 文本列，schema.sql 含安全迁移 do-block）；loadUserData 解析进 `cache._customCups`
- 首页 `.add-buttons` 改为动态 `#quickAddBtns` + `renderQuickAdd()`；补录弹窗同理由 `renderBackfillBtns()` 动态渲染
- 设置页「🚰 快速记录自定义」卡片：`renderCupsEditor()` 渲染行（图标/名称/容量输入），addCupRow（上限 8 提示）/removeCupRow/saveCustomCups（校验容量 1-5000、写库更新缓存）/resetCustomCups（写 null 恢复默认）
- renderRoute 进入设置页时渲染编辑器；admin 导出/导入与用户级导入均含 custom_cups

**需求（论坛）**：历史记录后新增「喝友论坛」导航页；用户可发帖、点赞、评论；顶部关键词查询 + 刷新。
**实现（论坛）**：
- schema.sql 新增三表：`forum_posts`(author/title≤60/content≤2000, FK→profiles 级联删)、`forum_likes`(post_id+username, unique 防重复赞)、`forum_comments`(post_id/username/content≤500)，均含 RLS public_all 策略与索引
- 导航加 `data-page="forum"`，PAGES 数组 + renderRoute 挂 `renderForumPage()`；新页面 `#page-forum`：page-head + 发帖按钮、搜索框+🔍查询+🔄刷新工具栏、帖子列表
- 数据流：`loadForumPosts()` 拉 posts(倒序 limit200)+likes+comments 明细 → 前端统计 likeCount/likedByMe/commentCount（延续 PGRST123 前端聚合方案）
- `toggleForumLike`：先查 likedByMe 判断 → delete().eq(post_id).eq(username) 或 insert，乐观更新按钮计数
- 评论：`openForumComments` 拉取该帖评论升序渲染；`addForumComment` 插入后重开弹窗刷新；计数同步更新
- 发帖 `submitForumPost`（标题+内容必填、长度校验）；`deleteForumPost` 仅作者本人（confirm 确认，DB 级联删赞/评）
- 查询 `searchForum` 客户端过滤标题/内容/作者；刷新 `refreshForum` 清空关键词重拉
- 弹窗：postOverlay/commentOverlay 复用 modal-overlay 样式，点击遮罩关闭，已注册 click 监听

**验证**：JS 语法检查通过；jsdom 冒烟测试 39 项全绿（导航/路由高亮/列表渲染倒序/点赞数评论数/自己帖子删除按钮/搜索过滤/刷新清空/点赞增删/发帖/评论/删帖/空结果提示）。
**坑**：jsdom 测试里 `resources:'usable'` 会真实加载 CDN supabase 并覆盖 window.supabase mock → 用真客户端 + 真 fetch 导致 ReferenceError；须去掉 resources 选项，仅靠 beforeParse 注入 mock。测试脚本 `test-forum.js` 保留在工作区可复用（内存 mock：select/eq/order/limit/maybeSingle/insert/update/delete 链式 thenable）。

## 荣誉墙（v11.5）

**需求**：导航新增【荣誉墙】（首页与历史记录之间）。①每日 01:00 生成昨日日榜 TOP3；每周一 01:00 上周周榜；每月 1 号 01:00 上月月榜——上榜者对应日/周/月榜勋章 +1。②每日 01:30 统计昨日达标（饮水量≥目标），达标勋章 +1。③关键：榜单/勋章生成后，用户补录旧数据不改历史结果（冻结）。④页面展示个人 4 类勋章累计 + 前 50 名勋章排名。

**实现**：
- schema.sql 新增：`honor_boards`(board_type/period/rank/username/amount, unique(board_type,period,username))、`honor_badges`(PK username+badge_type, count)、`honor_achieve`(unique(username,record_date))；RLS 只开放 SELECT（写入仅 postgres 定时任务）
- 生成函数：通用 `generate_honor_board(type,start,end,period)` + 日/周/月三个封装 + `generate_honor_achieve()`；**幂等核心**：`INSERT ... ON CONFLICT DO NOTHING RETURNING username`，仅新增成功才 upsert 勋章 +1 → 冻结 + 防重跑重复计分
- pg_cron 4 任务（**北京时间 = UTC+8**）：日榜/达标 `0 17 * * *`、`30 17 * * *`（前一自然日 17:00/17:30 UTC）；周榜 `0 17 * * 1`；月榜 `0 17 1 * *`；do-block 查 cron.job 表后 unschedule+schedule（幂等可重跑）
- 函数内日期一律 `(now() at time zone 'Asia/Shanghai')::date`：日榜昨日、周榜 today-7~today-1（周一运行）、月榜 date_trunc('month',today) 上下边界
- **安全**：`revoke all on function ... from public`，防匿名经 REST RPC 提前触发
- 前端：导航插入 honor（home 与 history 之间）、PAGES=['home','honor','history','forum','settings'] + renderRoute 挂 renderHonorPage；`#page-honor` = 我的勋章 5 卡（🥇日/🥈周/🥉月/✅达标 + 🌟总数）+ 勋章总榜 TOP50（7 列网格：排名/用户/日/周/月/达标/总数，前三🥇🥈🥉，自己高亮+「我」标签）+ 规则说明卡；`loadHonorWall` 拉 honor_badges 明细前端聚合（无 DB 聚合规避 PGRST123），总数降序 slice(0,50)

**验证**：JS 语法检查通过；北京时间周/月边界 + cron UTC 换算 Node 验证正确（跨年 2027-01、闰年 2 月边界都对）；jsdom 测试 20 项全绿（导航位置/路由/个人勋章计数/TOP50 排序/自己高亮/空数据/60 人截断 50）；论坛回归 39 项全绿。测试脚本 `test-honor.js` 保留。

**🐛 修复：pg_cron 美元引用嵌套报错**（用户执行报 `42601: syntax error at or near "select"` @LINE 327）
- 原因：`do $$ ... end $$;` 外层块内又用 `$$select ...$$` 包裹 cron 命令 → **同标签美元引用不能嵌套**，解析器把内层 `$$` 当作 do 块结束符，`select` 变成裸语句报错
- 修复：do 块改用带标签的 `$do$ ... $do$`（不同标签可任意嵌套），内部 cron 命令保持 `$$`；schema.sql 已加注释
- 验证：Node 模拟 PostgreSQL 美元引用解析（跳过注释/单引号），全部 28 个美元引用成对闭合、无嵌套冲突
- 教训：**写 PostgreSQL 含内嵌字符串的块时，外层用带标签定界符（$do$/$tag$），内层用 $$**；凡是在 SQL 里嵌套 $$ 必踩此坑

## 用户角色 + id 主键重构（v11.6）

**需求**：①用户分普通用户/管理员；②登录页「进入后台」入口取消，改为管理员登录后在导航栏用户区左侧显示「🛠 后台管理」按钮（普通用户不可见）；③profiles 增加用户 id，所有子表关联改用 id，弃用 username 外键。

**数据库（schema.sql 全量重写）**：
- profiles 新结构：`id uuid PK default gen_random_uuid()` + `username unique` + `role text default 'user' check in ('user','admin')`
- **存量库自动迁移**（幂等 do 块）：加 id/role 列 → 回填 uuid → 7 张子表（water_records/forum_posts/forum_likes/forum_comments/honor_boards/honor_badges/honor_achieve）加 user_id → join profiles 回填 → 清孤儿 → 切换 FK → 删 username 列 → 重建 unique/PK 约束（forum_likes unique(post_id,user_id)、honor_badges PK(user_id,badge_type)、honor_achieve unique(user_id,record_date)、honor_boards unique(board_type,period,user_id)）→ profiles 主键切到 id（先 drop 旧 username FK 再 drop profiles_pkey，最后 add PK(id)+username unique）
- **防提权**：`revoke insert/update (id, role) on profiles from anon, authenticated`（PostgREST 遵守列级权限）；honor 三表仍只读；生成函数改 user_id 并保持 revoke PUBLIC
- cron 定时任务不变（函数名/排程不变）；管理员设置注释在脚本末尾：`update profiles set role='admin' where username='...'`

**前端（index.html）**：
- 全局新增 `currentUserId`/`currentUserRole`/`userNameMap{}`；辅助 `loadUserNameMap()`（拉 profiles id,username 映射）+ `userName(id)`
- loadUserData 设置 currentUserId/currentUserRole；写操作全部 `user_id`（addWater/backfill/deleteRecord/clearToday/saveGoal/changePassword/saveCustomCups/resetCustomCups/importUserData）
- 登录页 admin-link 按钮删除、adminPwOverlay 删除、ADMIN_PASSWORD 变量删除（含 CSS）
- 导航栏 nav-user 前加 `#navAdmin` 按钮（`onclick="showAdminPanel()"`，默认隐藏，renderUserBadge 按 role 显示）
- 展示类聚合统一走 `loadUserNameMap` 映射（排行榜 user_id 分组 + 显示名、论坛帖子/评论作者、荣誉墙 TOP50、后台用户列表）
- 后台管理：confirmResetPw/confirmDeleteUser 改传 (userId, name)、按 id 操作；showAdminPanel 按 user_id 聚合 + 角色标签 `.au-role`（admin 红色）；admin 导出含 id/role（users 变对象数组），导入兼容旧备份（users 字符串数组 → 无 id 时 insert 后 `.select()` 回读生成的 id）；删除自己时同时清 currentUserId 等
- exportData 备份加 `_userId`

**验证**：JS 语法检查 + 标签配对通过；jsdom 三套测试全绿——test-forum.js 30 项、test-honor.js 16 项（均改 user_id mock）、test-role.js 25 项（登录页无后台入口/普通用户隐藏/管理员显示/后台面板聚合+角色标签/重置密码按 id/删除用户按 id/addWater 写 user_id/排行榜显示用户名/clearToday 按 user_id+date/登出重置）。
**坑**：测试 mock 的链式查询需支持 gte/lte（排行榜用）；`delete().neq('id', '00000000-0000-0000-0000-000000000000')` 用于 uuid 全表删除（旧 username 版用 neq('username','___NONE___')）。

**🐛 修复：主键迁移顺序错误**（用户执行报 `2BP01: cannot drop constraint profiles_pkey ... depends on index profiles_pkey`，7 张子表 username 外键全依赖它）
- 原因：初版脚本把「profiles 主键切换（drop profiles_pkey）」放在子表迁移**之前**——此时 7 张子表旧 username 外键还在引用 username 主键，删除被依赖对象报错
- 修复（正确的迁移顺序）：① profiles 加 id/role + 回填 + **先给 id 建 `profiles_id_key unique(id)`**（供子表新 user_id 外键引用）→ ② 7 张子表逐个迁移（drop 旧 username 外键 → 加 user_id 回填 → 建新 FK → 删 username 列 → 重建 unique/PK）→ ③ **最后**切主键（drop profiles_pkey → add PK(id) → username unique → 清理过渡的 profiles_id_key）
- 已验证：美元引用全配对；关键语句出现顺序递增（加列→id unique→子表迁移→主键切换→索引）
- 教训：**改主键前必须先摘除所有引用旧主键的外键；给新主键列先建唯一约束，子表新外键才能引用**；用户重跑整个 schema.sql 即可（第 1 节已生效部分幂等跳过）

**🐛 修复2：profiles_id_key 不能删**（用户执行报 `2BP01: cannot drop constraint profiles_id_key ... depends on index profiles_id_key`，7 张子表 user_id 外键依赖它）
- 原因：子表迁移时 user_id 外键绑定在过渡约束 profiles_id_key 上（当时 id 只有它一个唯一约束）；主键切换块最后 drop profiles_id_key → 外键仍依赖该索引 → 2BP01。do 块原子事务回滚了主键切换（主键仍 username），但子表迁移各自提交成功（username 列已删、user_id 外键已建）
- 修复：**保留 profiles_id_key 作为冗余唯一约束**，不再删除（删除需 CASCADE 会误删 7 个外键，得不偿失；冗余唯一索引对小应用无影响）。重跑时 add PK(id) 若复用该索引则约束自动转换，若新建索引则双唯一索引共存，均不报错
- 教训：**过渡期唯一约束被外键引用后就不能简单删除**；迁移类脚本尽量让中间约束"幸存"而不是清理，避免 2BP01

