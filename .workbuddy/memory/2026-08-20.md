# 2026-08-20 工作日志

## 喝水追踪器改造：本地版 → Vercel + Supabase 云端共享版

**用户决策**：用户名+密码登录（PBKDF2）、尚无 Supabase 项目（需完整指导）、旧数据不迁移。

**完成内容**：
- 新建 `index.html`：云端版应用（Vercel 部署入口）。核心改造：
  - 登录/注册改为用户名+密码，PBKDF2(100k 迭代+随机盐) 哈希，Web Crypto 原生实现
  - 数据层从 localStorage 改为 Supabase REST：内存缓存同步渲染 + 每次操作异步精确写库（增/删/改/查）
  - 记录主键改用数据库自增 id，操作全部映射 water_records/profiles 表
  - 后台管理改为数据库聚合（用户列表/汇总/7天日活），新增管理员重置密码功能
  - 未配置 Supabase 时登录页显示引导横幅
  - supabase-js 用 CDN UMD 引入，保持零构建、双击可用的单文件形态
- 新建 `supabase/schema.sql`：profiles + water_records 表、索引、RLS 策略（using(true)，应用层鉴权）
- 新建 `DEPLOY.md`：Supabase 建项目→执行 SQL→填配置→Vercel 部署全流程指南
- 更新 `README.md`：双版本架构说明（云端版推荐 / 本地版保留）
- 旧文件 `water-tracker.html`、`water-data.js` 保留不动

**验证**：内嵌 JS 语法检查通过；jsdom 冒烟测试通过（未配置/已配置两种场景均无报错）；HTML 标签配对完整。

**关键技术点**：
- PBKDF2 哈希格式 `pbkdf2$100000$salt$hex`
- PostgREST 删除全部记录用 `delete().neq('id', 0)` 技巧
- 全量备份格式向后兼容旧版（含 `_passwordHash` 字段保留密码）
- 旧格式备份导入时无密码字段 → 默认密码 water123（预览弹窗提示）
